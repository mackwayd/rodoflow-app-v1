package com.example.rodoflow.ui.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.ui.unit.dp

/** Cards principais (home, listas, financeiro, detalhe). */
val AppCardShape = RoundedCornerShape(18.dp)

/** Botões cheios / outlined primários. */
val AppButtonShape = RoundedCornerShape(14.dp)

/** Faixas de aviso ou info compactas. */
val AppBannerShape = RoundedCornerShape(12.dp)

/** Mini-cards (indicadores). */
val AppCompactCardShape = RoundedCornerShape(14.dp)
