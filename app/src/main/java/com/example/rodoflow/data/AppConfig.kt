package com.example.rodoflow.data

/**
 * Configuração de exibição no app do motorista.
 * Afeta apenas a camada visual — nenhum dado é removido do backend.
 */
object AppConfig {
    const val DAYS_VISIBLE_IN_APP = 30
}
